## Mono repository for quasar and vite POC

### To run in production mode:
#### Quasar
`cd quasar && npm i && npm run build && npm run start`

Access via localhost:3000

#### Vite
`cd vite && npm i && npm run build && npm run start`

Access via localhost:3001